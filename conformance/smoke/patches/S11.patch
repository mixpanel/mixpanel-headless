diff --git a/src/mixpanel_headless/_internal/services/live_query.py b/src/mixpanel_headless/_internal/services/live_query.py
index d599851..af420d0 100644
--- a/src/mixpanel_headless/_internal/services/live_query.py
+++ b/src/mixpanel_headless/_internal/services/live_query.py
@@ -199,7 +199,7 @@ def _transform_retention(
         counts = cohort_data.get("counts", [])
 
         # Calculate retention percentages
-        retention = [count / size if size > 0 else 0.0 for count in counts]
+        retention = [count / size if size > 0 else 1.0 for count in counts]
 
         cohorts.append(
             CohortInfo(
