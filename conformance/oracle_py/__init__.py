"""Python differential-oracle package (design D14).

Will contain ``__main__.py`` implementing the newline-delimited JSON-RPC 2.0
oracle protocol (``oracle.info`` / ``oracle.call`` / ``oracle.shutdown``)
over the D4 builder-side registry entries.

Populated by task PR-10; this package is scaffolded empty by PR-1.
"""
