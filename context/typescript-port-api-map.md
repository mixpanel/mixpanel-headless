# TypeScript Port — API Map (generated)

Generated by AST extraction from `workspace.py` + `__init__.py` on 2026-08-14.
This is the Phase 3 mechanical queue: one row per public Workspace member.
Batches reference Appendix B of `typescript-port-plan.md`. `done` = TS file exists + vectors green.

**Totals**: 200 public methods, 5 properties, 39 sections, 281 package exports.

## Workspace members

### LIFECYCLE & CONSTRUCTION  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `account` (L515) | `get account(): _AccountUnion` | property → getter (must stay sync — no I/O) or method |
| `project` (L520) | `get project(): _Project` | property → getter (must stay sync — no I/O) or method |
| `workspace` (L525) | `get workspace(): _WorkspaceRef | None` | property → getter (must stay sync — no I/O) or method |
| `session` (L530) | `get session(): _Session` | property → getter (must stay sync — no I/O) or method |
| `use` (L536) | `async use(account, project, workspace, …): Promise<Workspace>` |  |
| `close` (L725) | `async close(): Promise<void>` |  |

### WORKSPACE MANAGEMENT  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_workspaces` (L782) | `async list_workspaces(): Promise<list[PublicWorkspace]>` |  |
| `resolve_workspace_id` (L808) | `async resolve_workspace_id(): Promise<number>` |  |

### /ME & PROJECT DISCOVERY  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `me` (L867) | `async me(force_refresh): Promise<Any>` |  |
| `projects` (L894) | `async projects(refresh): Promise<list[_Project]>` |  |
| `workspaces` (L939) | `async workspaces(project_id, refresh): Promise<list[_WorkspaceRef]>` |  |

### DISCOVERY METHODS  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `events` (L1020) | `async events(limit, from_date, to_date): Promise<list[str]>` |  |
| `properties` (L1071) | `async properties(event): Promise<list[str]>` |  |
| `property_values` (L1087) | `async property_values(property_name, event, limit): Promise<list[str]>` |  |
| `subproperties` (L1113) | `async subproperties(property_name, event, sample_size): Promise<list[SubPropertyInfo]>` |  |
| `funnels` (L1174) | `async funnels(): Promise<list[FunnelInfo]>` |  |
| `cohorts` (L1187) | `async cohorts(): Promise<list[SavedCohort]>` |  |
| `list_bookmarks` (L1200) | `async list_bookmarks(bookmark_type): Promise<list[BookmarkInfo]>` |  |
| `top_events` (L1224) | `async top_events(type, limit): Promise<list[TopEvent]>` |  |
| `clear_discovery_cache` (L1254) | `async clear_discovery_cache(): Promise<void>` |  |

### LEXICON SCHEMA METHODS  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `lexicon_schemas` (L1266) | `async lexicon_schemas(entity_type): Promise<list[LexiconSchema]>` |  |
| `lexicon_schema` (L1296) | `async lexicon_schema(entity_type, name): Promise<LexiconSchema>` |  |
| `schema_graph` (L1327) | `async schema_graph(include_density, include_user_properties, force_refresh): Promise<SchemaGraphResult>` |  |

### STREAMING METHODS  ·  batch B4 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `stream_events` (L1381) | `stream_events(from_date, to_date, events, …): AsyncIterable<dict[str, Any]>` | streaming → AsyncIterable |
| `stream_profiles` (L1450) | `stream_profiles(where, cohort_id, output_properties, …): AsyncIterable<dict[str, Any]>` | streaming → AsyncIterable |

### LIVE QUERY METHODS  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `segmentation` (L1564) | `async segmentation(event, from_date, to_date, …): Promise<SegmentationResult>` |  |
| `funnel` (L1599) | `async funnel(funnel_id, from_date, to_date, …): Promise<FunnelResult>` |  |
| `retention` (L1631) | `async retention(born_event, return_event, from_date, …): Promise<RetentionResult>` |  |
| `event_counts` (L1675) | `async event_counts(events, from_date, to_date, …): Promise<EventCountsResult>` |  |
| `property_counts` (L1707) | `async property_counts(event, property_name, from_date, …): Promise<PropertyCountsResult>` |  |
| `activity_feed` (L1748) | `async activity_feed(distinct_ids, from_date, to_date, …): Promise<ActivityFeedResult>` |  |
| `query_saved_report` (L1827) | `async query_saved_report(bookmark_id, bookmark_type, from_date, …): Promise<SavedReportResult>` |  |
| `query_saved_flows` (L1863) | `async query_saved_flows(bookmark_id): Promise<FlowsResult>` |  |
| `frequency` (L1881) | `async frequency(from_date, to_date, unit, …): Promise<FrequencyResult>` |  |
| `segmentation_numeric` (L1916) | `async segmentation_numeric(event, from_date, to_date, …): Promise<NumericBucketResult>` |  |
| `segmentation_sum` (L1954) | `async segmentation_sum(event, from_date, to_date, …): Promise<NumericSumResult>` |  |
| `segmentation_average` (L1989) | `async segmentation_average(event, from_date, to_date, …): Promise<NumericAverageResult>` |  |

### INSIGHTS QUERY API  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `query` (L2266) | `async query(events, from_date, to_date, …): Promise<QueryResult>` |  |
| `build_params` (L2412) | `async build_params(events, from_date, to_date, …): Promise<Record<string, unknown>>` |  |

### Funnel Query  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `query_funnel` (L3045) | `async query_funnel(steps, conversion_window, conversion_window_unit, …): Promise<FunnelQueryResult>` |  |
| `build_funnel_params` (L3183) | `async build_funnel_params(steps, conversion_window, conversion_window_unit, …): Promise<Record<string, unknown>>` |  |

### FLOW QUERY  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `query_flow` (L3833) | `async query_flow(event, forward, reverse, …): Promise<FlowQueryResult>` |  |
| `build_flow_params` (L3969) | `async build_flow_params(event, forward, reverse, …): Promise<Record<string, unknown>>` |  |

### RETENTION QUERY  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `query_retention` (L4206) | `async query_retention(born_event, return_event, retention_unit, …): Promise<RetentionQueryResult>` |  |
| `build_retention_params` (L4330) | `async build_retention_params(born_event, return_event, retention_unit, …): Promise<Record<string, unknown>>` |  |

### ESCAPE HATCHES  ·  batch B4 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `api` (L4446) | `get api(): MixpanelAPIClient` | property → getter (must stay sync — no I/O) or method |

### DASHBOARD CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_dashboards` (L4487) | `async list_dashboards(ids): Promise<list[Dashboard]>` |  |
| `create_dashboard` (L4517) | `async create_dashboard(params): Promise<Dashboard>` |  |
| `get_dashboard` (L4548) | `async get_dashboard(dashboard_id): Promise<Dashboard>` |  |
| `update_dashboard` (L4577) | `async update_dashboard(dashboard_id, params): Promise<Dashboard>` |  |
| `delete_dashboard` (L4613) | `async delete_dashboard(dashboard_id): Promise<void>` |  |
| `bulk_delete_dashboards` (L4634) | `async bulk_delete_dashboards(ids): Promise<void>` |  |

### DASHBOARD ADVANCED OPERATIONS  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `favorite_dashboard` (L4659) | `async favorite_dashboard(dashboard_id): Promise<void>` |  |
| `unfavorite_dashboard` (L4680) | `async unfavorite_dashboard(dashboard_id): Promise<void>` |  |
| `pin_dashboard` (L4701) | `async pin_dashboard(dashboard_id): Promise<void>` |  |
| `unpin_dashboard` (L4722) | `async unpin_dashboard(dashboard_id): Promise<void>` |  |
| `remove_report_from_dashboard` (L4743) | `async remove_report_from_dashboard(dashboard_id, bookmark_id): Promise<Dashboard>` |  |
| `add_report_to_dashboard` (L4771) | `async add_report_to_dashboard(dashboard_id, bookmark_id): Promise<Dashboard>` |  |
| `list_blueprint_templates` (L4806) | `async list_blueprint_templates(include_reports): Promise<list[BlueprintTemplate]>` |  |
| `create_blueprint` (L4832) | `async create_blueprint(template_type): Promise<Dashboard>` |  |
| `get_blueprint_config` (L4861) | `async get_blueprint_config(dashboard_id): Promise<BlueprintConfig>` |  |
| `update_blueprint_cohorts` (L4890) | `async update_blueprint_cohorts(cohorts): Promise<void>` |  |
| `finalize_blueprint` (L4911) | `async finalize_blueprint(params): Promise<Dashboard>` |  |
| `create_rca_dashboard` (L4946) | `async create_rca_dashboard(params): Promise<Dashboard>` |  |
| `get_bookmark_dashboard_ids` (L4981) | `async get_bookmark_dashboard_ids(bookmark_id): Promise<list[int]>` |  |
| `get_dashboard_erf` (L5005) | `async get_dashboard_erf(dashboard_id): Promise<Record<string, unknown>>` |  |
| `update_report_link` (L5029) | `async update_report_link(dashboard_id, report_link_id, params): Promise<void>` |  |
| `update_text_card` (L5063) | `async update_text_card(dashboard_id, text_card_id, params): Promise<void>` |  |

### BOOKMARK/REPORT CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_bookmarks_v2` (L5101) | `async list_bookmarks_v2(bookmark_type, ids): Promise<list[Bookmark]>` |  |
| `create_bookmark` (L5196) | `async create_bookmark(params): Promise<Bookmark>` |  |
| `get_bookmark` (L5273) | `async get_bookmark(bookmark_id): Promise<Bookmark>` |  |
| `update_bookmark` (L5302) | `async update_bookmark(bookmark_id, params): Promise<Bookmark>` |  |
| `delete_bookmark` (L5359) | `async delete_bookmark(bookmark_id): Promise<void>` |  |
| `bulk_delete_bookmarks` (L5380) | `async bulk_delete_bookmarks(ids): Promise<void>` |  |
| `bulk_update_bookmarks` (L5401) | `async bulk_update_bookmarks(entries): Promise<void>` |  |
| `bookmark_linked_dashboard_ids` (L5424) | `async bookmark_linked_dashboard_ids(bookmark_id): Promise<list[int]>` |  |
| `get_bookmark_history` (L5448) | `async get_bookmark_history(bookmark_id, cursor, page_size): Promise<BookmarkHistoryResponse>` |  |

### COHORT CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_cohorts_full` (L5487) | `async list_cohorts_full(data_group_id, ids): Promise<list[Cohort]>` |  |
| `get_cohort` (L5523) | `async get_cohort(cohort_id): Promise<Cohort>` |  |
| `create_cohort` (L5552) | `async create_cohort(params): Promise<Cohort>` |  |
| `update_cohort` (L5583) | `async update_cohort(cohort_id, params): Promise<Cohort>` |  |
| `delete_cohort` (L5615) | `async delete_cohort(cohort_id): Promise<void>` |  |
| `bulk_delete_cohorts` (L5636) | `async bulk_delete_cohorts(ids): Promise<void>` |  |
| `bulk_update_cohorts` (L5657) | `async bulk_update_cohorts(entries): Promise<void>` |  |

### FEATURE FLAG CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_feature_flags` (L5684) | `async list_feature_flags(include_archived): Promise<list[FeatureFlag]>` |  |
| `create_feature_flag` (L5713) | `async create_feature_flag(params): Promise<FeatureFlag>` |  |
| `get_feature_flag` (L5744) | `async get_feature_flag(flag_id): Promise<FeatureFlag>` |  |
| `update_feature_flag` (L5773) | `async update_feature_flag(flag_id, params): Promise<FeatureFlag>` |  |
| `delete_feature_flag` (L5811) | `async delete_feature_flag(flag_id): Promise<void>` |  |

### FEATURE FLAG LIFECYCLE  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `archive_feature_flag` (L5836) | `async archive_feature_flag(flag_id): Promise<void>` |  |
| `restore_feature_flag` (L5857) | `async restore_feature_flag(flag_id): Promise<FeatureFlag>` |  |
| `duplicate_feature_flag` (L5882) | `async duplicate_feature_flag(flag_id): Promise<FeatureFlag>` |  |

### FEATURE FLAG OPERATIONS  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `set_flag_test_users` (L5911) | `async set_flag_test_users(flag_id, params): Promise<void>` |  |
| `get_flag_history` (L5936) | `async get_flag_history(flag_id, page, page_size): Promise<FlagHistoryResponse>` |  |
| `get_flag_limits` (L5976) | `async get_flag_limits(): Promise<FlagLimitsResponse>` |  |

### EXPERIMENT CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_experiments` (L6003) | `async list_experiments(include_archived): Promise<list[Experiment]>` |  |
| `create_experiment` (L6030) | `async create_experiment(params): Promise<Experiment>` |  |
| `get_experiment` (L6061) | `async get_experiment(experiment_id): Promise<Experiment>` |  |
| `update_experiment` (L6090) | `async update_experiment(experiment_id, params): Promise<Experiment>` |  |
| `delete_experiment` (L6126) | `async delete_experiment(experiment_id): Promise<void>` |  |

### EXPERIMENT LIFECYCLE  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `launch_experiment` (L6151) | `async launch_experiment(experiment_id): Promise<Experiment>` |  |
| `conclude_experiment` (L6176) | `async conclude_experiment(experiment_id, params): Promise<Experiment>` |  |
| `decide_experiment` (L6210) | `async decide_experiment(experiment_id, params): Promise<Experiment>` |  |

### EXPERIMENT MANAGEMENT  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `archive_experiment` (L6247) | `async archive_experiment(experiment_id): Promise<void>` |  |
| `restore_experiment` (L6268) | `async restore_experiment(experiment_id): Promise<Experiment>` |  |
| `duplicate_experiment` (L6293) | `async duplicate_experiment(experiment_id, params): Promise<Experiment>` |  |
| `list_erf_experiments` (L6330) | `async list_erf_experiments(): Promise<list[dict[str, Any]]>` |  |

### Annotations  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_annotations` (L6355) | `async list_annotations(from_date, to_date, tags): Promise<list[Annotation]>` |  |
| `create_annotation` (L6392) | `async create_annotation(params): Promise<Annotation>` |  |
| `get_annotation` (L6422) | `async get_annotation(annotation_id): Promise<Annotation>` |  |
| `update_annotation` (L6448) | `async update_annotation(annotation_id, params): Promise<Annotation>` |  |
| `delete_annotation` (L6479) | `async delete_annotation(annotation_id): Promise<void>` |  |
| `list_annotation_tags` (L6500) | `async list_annotation_tags(): Promise<list[AnnotationTag]>` |  |
| `create_annotation_tag` (L6524) | `async create_annotation_tag(params): Promise<AnnotationTag>` |  |

### Webhook CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_webhooks` (L6556) | `async list_webhooks(): Promise<list[ProjectWebhook]>` |  |
| `create_webhook` (L6580) | `async create_webhook(params): Promise<WebhookMutationResult>` |  |
| `update_webhook` (L6609) | `async update_webhook(webhook_id, params): Promise<WebhookMutationResult>` |  |
| `delete_webhook` (L6641) | `async delete_webhook(webhook_id): Promise<void>` |  |
| `test_webhook` (L6662) | `async test_webhook(params): Promise<WebhookTestResult>` |  |

### Alert CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_alerts` (L6696) | `async list_alerts(bookmark_id, skip_user_filter): Promise<list[CustomAlert]>` |  |
| `create_alert` (L6731) | `async create_alert(params): Promise<CustomAlert>` |  |
| `get_alert` (L6767) | `async get_alert(alert_id): Promise<CustomAlert>` |  |
| `update_alert` (L6793) | `async update_alert(alert_id, params): Promise<CustomAlert>` |  |
| `delete_alert` (L6822) | `async delete_alert(alert_id): Promise<void>` |  |
| `bulk_delete_alerts` (L6843) | `async bulk_delete_alerts(ids): Promise<void>` |  |
| `get_alert_count` (L6864) | `async get_alert_count(alert_type): Promise<AlertCount>` |  |
| `get_alert_history` (L6891) | `async get_alert_history(alert_id, page_size, next_cursor, …): Promise<AlertHistoryResponse>` |  |
| `test_alert` (L6933) | `async test_alert(params): Promise<Record<string, unknown>>` |  |
| `get_alert_screenshot_url` (L6964) | `async get_alert_screenshot_url(gcs_key): Promise<AlertScreenshotResponse>` |  |
| `validate_alerts_for_bookmark` (L6990) | `async validate_alerts_for_bookmark(params): Promise<ValidateAlertsForBookmarkResponse>` |  |

### Data Governance — Data Definitions / Lexicon  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `get_event_definitions` (L7034) | `async get_event_definitions(names): Promise<list[EventDefinition]>` |  |
| `update_event_definition` (L7064) | `async update_event_definition(event_name, params): Promise<EventDefinition>` |  |
| `delete_event_definition` (L7097) | `async delete_event_definition(event_name): Promise<void>` |  |
| `bulk_update_event_definitions` (L7118) | `async bulk_update_event_definitions(params): Promise<list[EventDefinition]>` |  |
| `get_property_definitions` (L7152) | `async get_property_definitions(names, resource_type): Promise<list[PropertyDefinition]>` |  |
| `update_property_definition` (L7192) | `async update_property_definition(property_name, params): Promise<PropertyDefinition>` |  |
| `bulk_update_property_definitions` (L7225) | `async bulk_update_property_definitions(params): Promise<list[PropertyDefinition]>` |  |
| `list_lexicon_tags` (L7269) | `async list_lexicon_tags(): Promise<list[LexiconTag]>` |  |
| `create_lexicon_tag` (L7307) | `async create_lexicon_tag(params): Promise<LexiconTag>` |  |
| `update_lexicon_tag` (L7333) | `async update_lexicon_tag(tag_id, params): Promise<LexiconTag>` |  |
| `delete_lexicon_tag` (L7362) | `async delete_lexicon_tag(tag_name): Promise<void>` |  |

### Data Governance — Drop Filters  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_drop_filters` (L7387) | `async list_drop_filters(): Promise<list[DropFilter]>` |  |
| `create_drop_filter` (L7410) | `async create_drop_filter(params): Promise<list[DropFilter]>` |  |
| `update_drop_filter` (L7441) | `async update_drop_filter(params): Promise<list[DropFilter]>` |  |
| `delete_drop_filter` (L7471) | `async delete_drop_filter(drop_filter_id): Promise<list[DropFilter]>` |  |
| `get_drop_filter_limits` (L7496) | `async get_drop_filter_limits(): Promise<DropFilterLimitsResponse>` |  |

### Data Governance — Custom Properties  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_custom_properties` (L7523) | `async list_custom_properties(): Promise<list[CustomProperty]>` |  |
| `create_custom_property` (L7568) | `async create_custom_property(params): Promise<CustomProperty>` |  |
| `get_custom_property` (L7604) | `async get_custom_property(property_id): Promise<CustomProperty>` |  |
| `update_custom_property` (L7630) | `async update_custom_property(property_id, params): Promise<CustomProperty>` |  |
| `delete_custom_property` (L7662) | `async delete_custom_property(property_id): Promise<void>` |  |
| `validate_custom_property` (L7683) | `async validate_custom_property(params): Promise<Record<string, unknown>>` |  |

### Data Governance — Lookup Tables  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_lookup_tables` (L7722) | `async list_lookup_tables(data_group_id): Promise<list[LookupTable]>` |  |
| `upload_lookup_table` (L7750) | `async upload_lookup_table(params, poll_interval, max_poll_seconds): Promise<LookupTable>` |  |
| `mark_lookup_table_ready` (L7905) | `async mark_lookup_table_ready(params): Promise<LookupTable>` |  |
| `get_lookup_upload_url` (L7945) | `async get_lookup_upload_url(content_type): Promise<LookupTableUploadUrl>` |  |
| `get_lookup_upload_status` (L7973) | `async get_lookup_upload_status(upload_id): Promise<Record<string, unknown>>` |  |
| `update_lookup_table` (L7998) | `async update_lookup_table(data_group_id, params): Promise<LookupTable>` |  |
| `delete_lookup_tables` (L8030) | `async delete_lookup_tables(data_group_ids): Promise<void>` |  |
| `download_lookup_table` (L8051) | `async download_lookup_table(data_group_id, file_name, limit): Promise<bytes>` |  |
| `get_lookup_download_url` (L8086) | `async get_lookup_download_url(data_group_id): Promise<string>` |  |

### Data Governance — Custom Events  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `create_custom_event` (L8115) | `async create_custom_event(params): Promise<CustomEvent>` |  |
| `list_custom_events` (L8156) | `async list_custom_events(): Promise<list[EventDefinition]>` |  |
| `update_custom_event` (L8179) | `async update_custom_event(custom_event_id, params): Promise<EventDefinition>` |  |
| `delete_custom_event` (L8233) | `async delete_custom_event(custom_event_id): Promise<void>` |  |

### Data Governance — Tracking & History  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `get_tracking_metadata` (L8269) | `async get_tracking_metadata(event_name): Promise<Record<string, unknown>>` |  |
| `get_event_history` (L8297) | `async get_event_history(event_name): Promise<list[dict[str, Any]]>` |  |
| `get_property_history` (L8324) | `async get_property_history(property_name, entity_type): Promise<list[dict[str, Any]]>` |  |
| `export_lexicon` (L8357) | `async export_lexicon(export_types): Promise<Record<string, unknown>>` |  |

### Schema Registry CRUD  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_schema_registry` (L8393) | `async list_schema_registry(entity_type): Promise<list[SchemaEntry]>` |  |
| `create_schema` (L8424) | `async create_schema(entity_type, entity_name, schema_json): Promise<Record<string, unknown>>` |  |
| `create_schemas_bulk` (L8457) | `async create_schemas_bulk(params): Promise<BulkCreateSchemasResponse>` |  |
| `update_schema` (L8491) | `async update_schema(entity_type, entity_name, schema_json): Promise<Record<string, unknown>>` |  |
| `update_schemas_bulk` (L8524) | `async update_schemas_bulk(params): Promise<list[BulkPatchResult]>` |  |
| `delete_schemas` (L8557) | `async delete_schemas(entity_type, entity_name): Promise<DeleteSchemasResponse>` |  |

### Schema Enforcement  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `get_schema_enforcement` (L8602) | `async get_schema_enforcement(fields): Promise<SchemaEnforcementConfig>` |  |
| `init_schema_enforcement` (L8632) | `async init_schema_enforcement(params): Promise<Record<string, unknown>>` |  |
| `update_schema_enforcement` (L8662) | `async update_schema_enforcement(params): Promise<Record<string, unknown>>` |  |
| `replace_schema_enforcement` (L8692) | `async replace_schema_enforcement(params): Promise<Record<string, unknown>>` |  |
| `delete_schema_enforcement` (L8724) | `async delete_schema_enforcement(): Promise<Record<string, unknown>>` |  |

### Data Auditing  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `run_audit` (L8748) | `async run_audit(): Promise<AuditResponse>` |  |
| `run_audit_events_only` (L8784) | `async run_audit_events_only(): Promise<AuditResponse>` |  |

### Data Volume Anomalies  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_data_volume_anomalies` (L8821) | `async list_data_volume_anomalies(query_params): Promise<list[DataVolumeAnomaly]>` |  |
| `update_anomaly` (L8850) | `async update_anomaly(params): Promise<Record<string, unknown>>` |  |
| `bulk_update_anomalies` (L8878) | `async bulk_update_anomalies(params): Promise<Record<string, unknown>>` |  |

### Event Deletion Requests  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_deletion_requests` (L8911) | `async list_deletion_requests(): Promise<list[EventDeletionRequest]>` |  |
| `create_deletion_request` (L8932) | `async create_deletion_request(params): Promise<list[EventDeletionRequest]>` |  |
| `cancel_deletion_request` (L8967) | `async cancel_deletion_request(request_id): Promise<list[EventDeletionRequest]>` |  |
| `preview_deletion_filters` (L8991) | `async preview_deletion_filters(params): Promise<list[dict[str, Any]]>` |  |

### QUERY USER ENGINE  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `query_user` (L9417) | `async query_user(where, cohort, properties, …): Promise<UserQueryResult>` |  |
| `build_user_params` (L9578) | `async build_user_params(where, cohort, properties, …): Promise<Record<string, unknown>>` |  |

### Markdown documentation that grounds AI assistants in the  ·  batch B6 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `get_business_context` (L10099) | `async get_business_context(level, organization_id): Promise<BusinessContext>` |  |
| `set_business_context` (L10175) | `async set_business_context(content, level, organization_id): Promise<BusinessContext>` |  |
| `clear_business_context` (L10262) | `async clear_business_context(level, organization_id): Promise<BusinessContext>` |  |
| `get_business_context_chain` (L10306) | `async get_business_context_chain(): Promise<BusinessContextChain>` |  |

### Session Replay  ·  batch B5 · pkg core

| Python member | TS signature sketch | Notes |
|---|---|---|
| `list_replays` (L10373) | `async list_replays(distinct_id, replay_ids, from_date, …): Promise<list[ReplaySummary]>` |  |
| `events_for_replay` (L10446) | `async events_for_replay(replay_id, event_properties, from_date, …): Promise<list[ReplayEvent]>` |  |
| `events_for_replays` (L10484) | `async events_for_replays(replay_ids, event_properties, from_date, …): Promise<dict[str, list[ReplayEvent]]>` |  |
| `sign_replay` (L10521) | `async sign_replay(replay_id, env): Promise<SignedReplay>` |  |
| `sign_replays` (L10543) | `async sign_replays(replay_ids, env): Promise<list[SignedReplay]>` |  |
| `fetch_replay` (L10564) | `async fetch_replay(replay_id, distinct_id, env, …): Promise<Replay>` |  |
| `stream_replay` (L10672) | `stream_replay(replay_id, env, retention_days, …): AsyncIterable<dict[str, Any]>` | streaming → AsyncIterable |
| `fetch_replays` (L10734) | `async fetch_replays(replay_ids, env, max_files, …): Promise<ReplayBundle>` |  |
| `replays_for_user` (L10875) | `async replays_for_user(distinct_id, from_date, to_date, …): Promise<ReplayBundle>` |  |
| `analyze_replay` (L10940) | `async analyze_replay(replay_id): Promise<string>` |  |

## Package exports (281) by category

| Category | Count | Members |
|---|---|---|
| Workspace/facade | 5 | `Workspace`, `accounts`, `session`, `targets`, `login_unified` |
| Auth & accounts | 8 | `Account`, `OAuthBrowserAccount`, `OAuthTokenAccount`, `Region`, `ServiceAccount`, `Project`, `Session`, `WorkspaceRef` |
| Exceptions | 26 | `AccountInUseError`, `MixpanelHeadlessError`, `BookmarkValidationError`, `ValidationError`, `APIError`, `ConfigError`, `InvalidArgumentError`, `AccountNotFoundError`, `AccountExistsError`, `ProjectNotFoundError`, `AuthenticationError`, `RateLimitError`, … (+14) |
| Result/param types | 92 | `AccountSummary`, `AccountTestResult`, `OAuthLoginResult`, `SegmentationResult`, `FunnelResult`, `FunnelResultStep`, `RetentionResult`, `CohortInfo`, `FunnelInfo`, `SubPropertyInfo`, `EventCountsResult`, `PropertyCountsResult`, … (+80) |
| Literal aliases & enums | 140 | `AccountType`, `Target`, `TimeUnit`, `HourDayUnit`, `QueryTimeUnit`, `CountType`, `MathType`, `PerUserAggregation`, `FunnelMathType`, `ConversionWindowUnit`, `FunnelOrder`, `FunnelMode`, … (+128) |
| Replay | 6 | `Replay`, `ReplayBundle`, `ReplayEvent`, `ReplaySummary`, `SignedReplay`, `UserAction` |
| Other | 4 | `validate_bookmark`, `default_label_fn`, `selector_label_fn`, `url_normalizer` |

Full machine-readable map: `typescript-port-api-map.json` (same directory).
