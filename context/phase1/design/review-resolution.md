# Review Resolution Log (Arbiter)

Date: 2026-08-14. Input: review-extraction.md, review-judge.md, review-ts.md (findings JSON from orchestrator).
Verdicts: APPLIED (design edited), REJECTED (with reason), MERGED (duplicate of another finding, resolved together).
Evidence re-verified independently before every verdict; file:line cites below are my own checks, not reviewer copies.

## Verification notes (raw evidence checks)

- E2/J8: tests/integration/test_cross_project_iteration.py:157-180 confirmed — `projects()` + 3x `use(project=)` + 3x `events()` in one test, asserts zero /me during loop (cross-call contract). VERIFIED.
- E4/T3: src/mixpanel_headless/workspace.py:7857-7860 confirmed (`deadline = time.monotonic() + max_poll_seconds`, `while ... : time.sleep(poll_interval)`). freezegun freezes monotonic; plain no-op sleep => infinite loop. VERIFIED.
- E9: bookmark_builders.py:202 (bare-name `build_filter_entry` in section loop), :563 (recursive `sub_entry = build_filter_entry(sub)`). VERIFIED.
- E8: tests/unit/test_api_client.py:2795-2843 — handlers return `httpx.Response(200, stream=_IterableByteStream(chunks))` through `httpx.MockTransport` via raw `httpx.Client`; design's "bypass MockTransport" claim was false. VERIFIED.
- E3: grep census: `username="u"` x101, `SecretStr("s")` x91, `username="test_user"` only x56, team.sa/brw-tok/my-oauth-token etc. x51. Fixed-literal table would abort immediately. VERIFIED.
- J5: rulebook R10.11 (lines 282-291) is explicitly segfilter-operand-scoped; R10.12 says filterValue passed as-is, "a string would be quoted into the selector as a string literal"; bookmark_builders.py:~501 emits `"filterValue": f._value` raw. Rule 4b had no server-coercion evidence. VERIFIED.
- J7: tests/unit/test_pagination.py:444 and :693 patch `time.sleep` (whole Retry-After class :705-815 feeds through :693); paginator retry loop at src/.../pagination.py:168 (`for attempt in range(MAX_RATE_LIMIT_RETRIES + 1)`). api_client timing tests use monkeypatch sleep-capture at test_api_client.py:3494 (invisible to a `patch("time.sleep")` detector). VERIFIED.
- J3: pyproject.toml — freezegun would land in `[project.optional-dependencies].dev`; bare `uv run` in a worktree installs base deps + `[dependency-groups].dev` (click-man, pre-commit, psutil, ruff, types-psutil — no pytest, no freezegun). VERIFIED.
- T1: context/typescript-port-api-map.json parsed — keys = {generated, workspace_members (205), exports (281 bare strings)}; zero api_client/replays/oauth entries. Also untracked (git status `??`). VERIFIED.
- T2: api_client.py:7606 `upload_to_signed_url(url, csv_bytes: bytes)`; `download_lookup_table -> bytes` (:7855-7861). Schema had json_body/body_text only; codec list had no bytes. VERIFIED.
- T6: api_client.py:1856 sets `"Accept-Encoding": "gzip"` explicitly on the export stream. VERIFIED.
- T8: tests/unit/test_bookmark_builders.py:54-56 `patch("mixpanel_headless._internal.bookmark_builders.date")`, `mock_date.today.return_value = date(2025, 6, 15)`. VERIFIED (only module-clock-patching file per grep).
- E6/T11(3): duplicate method names across classes confirmed (e.g. tests/test_user_builders.py:101 and :117 both `test_integer_value`); slug omitting class/module collides. VERIFIED (spot-check; accepted reviewer census of 838/60 as order-of-magnitude).
- E10: tests/unit/test_api_client.py:1408-1452 `export_events(..., on_batch=on_batch)`. VERIFIED.
- E7: plan line 409 "Target: >=3,000 vectors extracted" is a Phase-1 step-1 target, separate from the Gate line; D10's arithmetic (1,400 + 1,700 => >=3,000) did not net its own exclusions. VERIFIED as stated.
- E11: tests/unit/test_schema_graph.py: 38 tests, 1 CliRunner use, 2 MockTransport sites — file-level CLI exclusion would drop 37+ library tests. VERIFIED.
- J2/J4/J6/J9/J10/J11, E5, E12, E13, T4, T5, T7, T9, T10, T12, T13, T14: verified textually against phase1-design.md / vector.schema.json / naming-map.md as quoted by reviewers (design self-contradictions or unspecified behavior; no repo evidence disputed).

## Resolution table

All 38 findings verified substantively correct; 36 APPLIED directly, 2 MERGED into their duplicates (J1→E1, J8→E2). Zero rejected. No gate weakened: the ≥3,000 target, 100%-pass criteria, drift byte-diff, and smoke-catch criteria all stand — the fixes make them ACHIEVABLE and HONEST, not looser (the smoke criterion got STRICTER: crashes now fail the smoke test instead of masquerading as catches).

| # | Reviewer | Severity | Finding (short) | Verdict | What changed |
|---|---|---|---|---|---|
| E1 | extraction | blocker | No wire capture layer for call.api/input/result | APPLIED | D1.2 rewritten: registry now has `wire_api`/`wire_state` entry classes covering every public MixpanelAPIClient/Workspace/ReplaysService method + OAuthFlow.refresh + probe_region, capturing (api, kwargs-via-codecs, return-or-raise) with thread-local span attribution of transport interactions; explicit anti-tautology note (to_date default never reverse-engineered from requests). PR-2/PR-3 tasks updated. |
| E2 | extraction | blocker | One-vector-per-test breaks on multi-CALL tests | APPLIED | D2 gains the setup/measured-call model: `call.setup[]` (ordered earlier entry-point calls incl. state mutators), measured call = last entry-point call; interactions[] spans setup+measured; known limitation (setup returns not diffed) logged + `counts.with_setup` manifest field. Schema: `call.setup[]` added. D7/D12 replay setup then measured. Chose judge's setup[] over extraction's calls[] — smaller schema delta, same replay fidelity for the cited cases. |
| E3 | extraction | blocker | Fixed credential table aborts on first nonstandard fake cred | APPLIED | D5 rules rewritten session-relative: auth-header pattern GENERATED from the bound session's credentials; abort only when authorization does not decode to the bound session's creds; rule-4 entropy/length denylist is the real-secret tripwire; SecretStr rule 5 aligned. Schema session comments updated. |
| E4 | extraction | blocker | freezegun + sleep no-op breaks monotonic poll loop | APPLIED | D1.4: explicit monotonic policy — VIRTUAL sleep (`time.sleep(d)` → freezegun `tick(d)`), monotonic frozen AND advanced by sleeps, so the poll-timeout loop (workspace.py:7857-7860) terminates deterministically; empirical PR-2 pilot check on test_workspace_data_governance.py made a done-criterion; Risk #2 row gains faulthandler_timeout backstop. Both runners install the same shim (D7/D12). |
| E5 | extraction | major | Position-based VectorTransport vs unordered_group | APPLIED | D2: emit-side canonical sort of group members + keyed replay serving; D6 rule 9: same sort at emit time; D7 VectorTransport + D12 VectorFetch: serve by (method, path, params) within unordered groups, each interaction consumable once. |
| E6 | extraction | major | Vector id slug omits test class (838 collisions) | APPLIED | D3: slug derived from FULL nodeid (module + class + function + param id); global collision pass appends sha1(nodeid) prefix to ALL colliders (covers T11's case-only `[A]`/`[a]` clash too); duplicate final ids = emit-time hard error. Schema id comment updated. |
| E7 | extraction | major | >=3,000 arithmetic ignores own exclusions | APPLIED | D10: un-netted arithmetic replaced by a count-reconciliation ledger (builder ~1,500 net, wire ~1,330-1,360 net, floor ~2,850 est., inflators named but unbudgeted, authored budget ~60-80). Target ≥3,000 NOT lowered: PR-5 done = ledger reconciles AND ≥3,000; a clean-reconciling shortfall is escalated to the orchestrator with the ledger, never filler-authored or silently passed. |
| E8 | extraction | major | _IterableByteStream "bypasses MockTransport" claim false | APPLIED | D1.1: stream capture is a TEE (record chunks as the consumer yields; never read-before-iteration, which would raise StreamConsumed); D1.3/D2: raw-httpx tests reclassified `raw_transport_no_entrypoint` (new D10 category) since they have no library entry point to replay; chunk contract stays covered by authored `_iter_jsonl_lines` vectors. |
| E9 | extraction | major | Nested/recursive registry capture inflation | APPLIED | D1.2: re-entrancy guard (thread-local depth; outermost test-invoked call wins), with the bookmark_builders.py:202/:563 evidence and the workspace.py:70 static-import asymmetry note; D3 notes ordinals count only outermost captures. |
| E10 | extraction | major | Callback kwargs unserializable, no policy | APPLIED | D4.4: `{"$type":"callback"}` codec — both runners inject recording stubs; call logs diffed against new schema field `expect.callback_calls` (preserves the on_batch retry-state-reset contract, test_api_client.py:1408-1452); `unserializable_input` D10 backstop category for anything else. |
| E11 | extraction | minor | File-level CLI exclusion drops ~70+ library tests | APPLIED | D10 `cli` row: per-test runtime detection (CliRunner.invoke observed inside the test), with the mixed-file evidence inlined. |
| E12 | extraction | minor | S12 justification cites nonexistent D10 category | APPLIED | S12 note re-justified on measured grounds (resolver-precedence tests mostly fire no seam → `no_seam_hit`, measured at PR-5, not assumed); dangling "env-dependent tests (D10)" citation removed; resolver probe renumbered optional 14th. |
| E13 | extraction | minor | Dual-seam tests silently drop builder captures | APPLIED | D1.3: per-capture classification — dual-seam tests emit BOTH wire and builder vectors (`-N` ordinals); nothing silently dropped. |
| J1 | judge | blocker | Wire vectors: no capture seam | MERGED→E1 | Same defect; judge's extra evidence (to_date-default tautology, 6/12 smoke patches wire-only) folded into the D1.2 rationale text. |
| J2 | judge | blocker | Drift check fails every PR (source_commit + authored) | APPLIED | D3: `source_commit` injected via `--mp-record-commit` (never `git rev-parse` at record time); D8: drift command reads date+commit back from the committed manifest; `conformance.record.diff` scope = extracted subset only (authored/** and enums/** excluded by path). |
| J3 | judge | major | Smoke worktree lacks freezegun; crash vs catch conflated | APPLIED | D9.2: mandatory `uv sync --all-extras` bootstrap per worktree (freezegun is a dev extra; bare `uv run` installs only dependency-groups.dev — verified against pyproject.toml); control run in an identical unpatched worktree, never the main checkout; D9.3: `vector_failed` vs `runner_crashed` distinguished, ANY crash = smoke ERROR (criterion made stricter); D7 CLI note fixed (it is NOT dep-free; fails fast without freezegun). |
| J4 | judge | major | Worktree pinned to pre-runner commit | APPLIED | D9.2 step 1: worktree at the same ref as the control run (rig branch HEAD), with the D16 commit-order rationale; corpus stays self-pinned via manifest source_commit. |
| J5 | judge | major | D6 rule 4b over-normalizes bookmark filterValue | APPLIED | Rule 4b DELETED; rule 4 now covers segfilter operand positions only (R10.11's actual evidence scope), with an explicit not-normalized note for filterValue (any dialect, per R10.12 + bookmark_builders raw passthrough) and engage selectors. |
| J6 | judge | major | Rule 3 / PRECISION_LOSS unimplementable via JSON.parse | APPLIED | D6 rule 3 restated over RAW JSON number tokens; TS loader REQUIRED to use lossless number parsing (hard TS-4 requirement, also in D12 loader bullet); PRECISION_LOSS paragraph restated in implementable terms. |
| J7 | judge | major | sleep-patch heuristic strips paginator-429 coverage | APPLIED | D10 layer3_deferred rule changed to "asserts on captured sleep DURATIONS" (test_api_client.py:3494-style), explicitly keeping pagination-429 sequence vectors; NEW smoke patch S13 (pagination.py:168 retry-loop removal) added to D9.1 + PR-8; both runners install virtual/no-op sleep at replay (D7/D12). |
| J8 | judge | major | Multi-call wire tests + set_workspace_id client state | MERGED→E2 | `wire_state` registry entries + setup[] replay reproduce client-attribute state by re-executing the public mutator (never snapshotting privates); D2 text carries the api_client.py:1418 evidence. |
| J9 | judge | minor | Error stripping reaches into details_contain | APPLIED | D6 rule 6: strip message/suggestion/fix ONLY at top-level error object + errors[] elements; never recursive into details_contain (exceptions.py:154 response_body rationale inlined). |
| J10 | judge | minor | Drift diff directionality unspecified | APPLIED | D8: normative diff spec — bidirectional bundle-path set equality, per-vector-id set equality, per-line + header byte equality; any asymmetry fails. |
| J11 | judge | minor | D8 job YAML omits D17-promised steps | APPLIED | D8 job YAML gains mypy-conformance + conformance/tests steps; `just conformance` recipe includes conformance/tests so `just check` stays a strict superset (single recipe/job matrix stated). |
| T1 | ts | blocker | api-map cannot resolve non-Workspace call.api | APPLIED | New `conformance/vectors/api-index.json` sidecar (D4.4, emitted by emit.py: every call.api + registry kind + positional/kw-only signature) is the second generator input for api-map.gen.ts (D12); UNPORTED "module known" universe derives from it; naming-map §4/§5 updated; D16 commit 1 commits the untracked api-map JSON with sha256 stamped into the generated file. Depends on E1's wire registry (the index is generated from it). |
| T2 | ts | major | bytes values unrepresentable end-to-end | APPLIED | `{"$type":"bytes",base64}` codec in both tables (D4.4, PR-3, D12); `body_base64` added to expectedRequest AND givenResponse in the schema; D6 rule 11 defines the expect.result encoding for bytes returns. |
| T3 | ts | major | file_path-dependent vectors unreplayable; poll-loop hang | APPLIED | New D10 category `fs_dependent` (upload_lookup_table facade tests excluded; lookup-table wire coverage retained via the api_client-level bytes-based 3-step vectors). Hang half resolved by E4's virtual-monotonic sleep + PR-2 pilot verification + faulthandler_timeout backstop (Risk #2). Chose exclusion over a `$type:file` codec: smaller surface, no fs semantics forced into the isomorphic runner. |
| T4 | ts | major | Hello-world gate proves only builder path | APPLIED | D13: ~8 authored wire-stub vectors against a ~30-line stub client registered in the api-map (mirror stub in pycompat_ref.py so both runners execute them); gate criteria extended to six, incl. a wire-path deliberate break (FAIL_REQUEST/FAIL_ERROR); TS-6/PR-7 updated. |
| T5 | ts | major | D15a feed contradicts insights-only scope | APPLIED | Feed restricted to capability ∈ {segmentation, bookmarks-insights}; funnels/flows/retention route exclusively to the D15b handoff JSONL, with the false-REJECT rationale inlined. |
| T6 | ts | major | headers_contain emission policy unspecified | APPLIED | D5 rule 6: normative emit-time allowlist (authorization pattern, library-set content-type, test-injected custom headers, non-default accept-encoding); transport-added headers never emitted; `headers_node_only` schema field for the browser-impossible `accept-encoding: gzip` (api_client.py:1856); canonical-selftest ignored-header case added (PR-4). |
| T7 | ts | major | Corpus/api-map coupled to mutable working tree | APPLIED | D12: corpus SNAPSHOT committed into the TS repo via scripts/sync-corpus.sh (verifies manifest.source_commit against the pin); api-map JSON committed in Python repo (D16); Discrepancy Log #12 records the deviation from plan §4.1's submodule-or-tarball wording. |
| T8 | ts | major | Test-local clock mocks poison date-builder vectors | APPLIED | D1.2: mock-on-module-clock-attr detection → no emission, manifest category `test_local_clock` (D10); authored RECORD_EPOCH replacements for build_date_range/build_time_section in PR-7; D4.2 item 2 cross-referenced. |
| T9 | ts | minor | ajv-draft-04 mislabel in D11 | APPLIED | D11 tooling line corrected: ajv 8.x as Ajv2020 strict:false; NO ajv-draft-04 (draft-04 schemas are Python-side in D15b), with the silent-prefixItems trap cited. |
| T10 | ts | minor | VectorFetch throw shape bypasses R2.10 mapping | APPLIED | D12: VectorFetch rejects as native fetch does (TypeError with cause) via committed transport-errors.ts table, keeping the adapter's R2.10 mapping (and UPLOAD_ERROR wrapping, api_client.py:7646-7651) under test. |
| T11 | ts | minor | Naming-map mechanics (domain defaults, ts_signature, id case collision) | APPLIED | naming-map §3: machine-decidable domain-default rule ($type-tagged wire-shaped values = keep; everything else = mechanical camel; overrides are explicit kwarg-scoped rows); §4: ts_signature declared non-normative; id case collision covered by E6's collision-hash pass. |
| T12 | ts | minor | -0.0 selftest / json_body null / body exclusivity | APPLIED | D6 rule 5: -0.0 renders "-0.0" (sign-preserving, TS special-case; mandatory selftest case, PR-4/D13); schema: "no body" = key absence (json_body:null reserved for a literal null body per R3.5); request AND response body-field mutual exclusivity enforced via allOf/not pairs, not $comment. |
| T13 | ts | minor | Bridge lone-surrogate encoding edge | APPLIED | D14: ensure_ascii=True framing mandated both directions; D6 rule 2: lone surrogates illegal in vectors (encoder rejects at record time, like NaN/Infinity) — protocol-level error, never a mid-session crash. (Reviewer's UnicodeEncodeError mechanism holds only under ensure_ascii=False; fix is correct either way and closes both paths.) |
| T14 | ts | minor | Injected-fetch plan deviation unlogged | APPLIED | Discrepancy Log #11 added (injected-fetch supersedes plan §4.1 MockAgent/MSW wording; MSW/MockAgent still allowed in ordinary Layer-3 tests). |

## Ripple check

Chased consequences of the primary fixes through the rest of the design:
- E1/E2 → D1.3 (per-capture classification), D3 (`-N` semantics), D4.4 (api-index generated from wire registry), D7/D12 (setup replay, callback diffing), D14 (oracle-py v1 surface explicitly EXCLUDES wire_api entries), PR-2/PR-3/TS-4/TS-5 task text, schema $comment extension list (items 9-11).
- E4/J7 → D7 clock bullet (virtual sleep at replay), D12 VectorFetch (sleep shim), Risk #2 (hang backstop), D9 patch count 12→13 everywhere (D9 intro, D9.2, D9.3, PR-8, worktree-cycle count 14 = control + 13).
- J2 → D3 manifest field text, D8 command line (`--mp-record-commit`), diff-scope spec.
- J3 → D7 CLI dependency note (freezegun fail-fast), D9.2 bootstrap step.
- E8 → D2 streaming paragraph (raw-httpx reclassification), D10 new category row.
- T1/T7 → naming-map §4/§5, D16 commits (api-map JSON in commit 1; TS commit 4 includes corpus snapshot), D12 verdict list gains UNMAPPED_API for consistency with naming-map §4.
- T4 → PR-7 (wire-stub vectors authored Python-side), pycompat_ref.py scope, TS-6 six criteria.
- J5/J6/T12 → D13 criterion 3 wording (selftest incl. -0.0), PR-4 selftest case list.
- T5 → no D15b change needed (handoff JSONL already carries funnels/common bookmark_type).
- Schema validated: `uv run python -c "json.load(...)"` → OK.
- Grep sweep for stale text (`S1–S12`, `12-patch`, `bypass MockTransport`, `known test credential`, `ajv-draft-04`, old vectorsPath) → clean; remaining `ajv-draft-04` mention is the corrected negative statement.

## Human calls needed

None. Every finding was decidable from rulebook + plan + repo evidence (R10.3 bar not met by any). The four pre-existing Escalations in phase1-design.md stand unchanged; one conditional escalation path was ADDED inside D10 (extraction total < 3,000 after clean ledger reconciliation → report to orchestrator), which is a defined future trigger, not an open ambiguity now.
