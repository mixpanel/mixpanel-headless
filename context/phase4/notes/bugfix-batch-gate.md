# Bugfix-batch GATE (R10.7 four-bug Python-first batch) — CLOSED, all clean

Date: 2026-08-17. Task: batch gate + PR preparation (NO pushes / NO PR
creation — the orchestrator does those). Normative spec: inbound-ledger row 2
re-pin choreography + playbook P3-7 trigger 3. Rulebook R10.7 / R10.2 / R5.4
discipline verified throughout by the two review pairs and their arbiters.

## 0. Final state (the numbers)

| Item | Value |
|---|---|
| New corpus pin | `700db996cc952e02aa5a23db1f3c68a3e7251b5b` (was `70c904dc`) |
| Corpus N | **3,262** (was 3,251; Δ11 = 20 modified + 11 added + 0 removed) |
| Python conformance @ pin | **3,262 / 3,262 PASS / 0 FAIL** (runner exit 0) |
| TS conformance @ pin | **3,262 / 3,262 PASS / 0 FAIL / 0 UNPORTED** @ 700db996cc95 — same N |
| Python `env -u FORCE_COLOR -u COLORTERM just check` | GREEN, EXIT=0 (lint, fmt-check, typecheck, docstring-cov, test-cov ≥90%, conformance, build — sdist+wheel 0.2.1) |
| TS `npm run check` | GREEN, EXIT=0 (typecheck ×5, eslint, prettier, **9,988 tests / 243 files**, browser smoke 2,069,640 bytes) |
| Differential regression | **11 seeds × 28,091 examples / 0 skips / 0 divergences** (fresh 741097477 + the full 10-seed B9 gate set), 55 families, both bridges `source_commit 700db996`, protocol 1.1 |
| Referee (a) ajv | 9/9 tests, 214 fed vectors, **0 REJECT** — 5 dataGroupId pins retired (no allowlist) |
| Referee (b) structural | **314/314 ACCEPT, 0 REJECT**, exit 0; handoff regenerated BYTE-IDENTICAL (314 entries) |
| Referee (b) deep | **125 ACCEPT / 0 REJECT / 189 SKIP_NON_INSIGHTS**, exit 0 — 2 standing frequency REJECTs retired |
| Python work branch | `ts-port/python-bugfix-batch`, stacked on `ts-port/phase2-contract-support` @ `c2a25c5` |
| TS branch | `main` (remote github.com/jaredmixpanel/mixpanel-headless-ts; 3 batch commits ahead of origin/main) |

Final HEAD SHAs (including this gate's docs commit) are recorded in the
orchestrator report; the pre-gate content HEADs were Python `db8a33e` /
TS `da68958`, and this gate adds DOCS-ONLY commits (no code, no vectors —
all verification above ran against the same trees the docs describe).

## 1. Per-bug summary

### (a) Frequency-filter clause shape

- **Root cause**: `build_frequency_filter_entry` emitted a home-grown nested
  frequency clause; the platform-native shape is top-level
  `filterType`/`filterOperator`/`filterValue` with `$frequency` under
  `behavior.behaviorType`, event filters in `behavior.filters`, label in
  top-level `value`. Disclosed since Phase 1 as the 2 referee-(b) deep
  REJECTs (true positives). Fix-of-record:
  `context/phase1/addendum/frequency-filter-probe.md` (+
  `context/phase1/bug-reports/mixpanel-headless-frequency-filter-clause-shape.md`).
- **Fix**: Python `bddc576` (FIX-1), red run 14F/1P before implementation.
- **Vector Δ**: 13 modified + 1 added (`test_no_custom_property_nesting`).
- **Twin retirement**: TS `2b72ce1` — `buildFrequencyFilterEntry` rewritten,
  same change as the test/vector flip.

### (b) dataGroupId int-vs-string threading

- **Root cause**: `data_group_id` emitted as `int` at clause level
  (`GroupClause.dataGroupId`, contract `string|null`), interior cohort-entry
  level, and sections level — where the contract key is
  `globalDataGroupId: string`, not `dataGroupId` (ajv `Sections` is
  `additionalProperties: false`). The pinned expected-and-disclosed ajv
  REJECT set. Fix-of-record:
  `context/phase3/bug-reports/mixpanel-headless-datagroupid-int-clause.md`.
- **Fix**: Python `bddc576` (FIX-1) — `str()` at emission, params stay
  `int | None`; sections rename to `globalDataGroupId`. Red run 9F/7P.
- **Vector Δ**: 7 modified + 0 added.
- **Twin retirement**: TS `2b72ce1` — `String()` coercions +
  `globalDataGroupId` at the 3 sections sites; the 5 ajv disclosure pins
  DELETED in the same commit.

### (c) `_handle_response` 403 TypeError

- **Root cause**: the 403 sensitive-data sniff assumed dict/str bodies;
  truthy non-dict/non-str JSON (`42`, `1.5`, `true`) crashed with an uncoded
  TypeError, and list bodies used element membership instead of substring
  sniffing. Fix-of-record:
  `context/phase3/bug-reports/python-handle-response-403-typeerror.md`.
- **Fix**: Python `57c5e16` (FIX-2) — uniform substring semantics over
  `str | "" | json.dumps(body)`; no error CODE changed (R5.4). Red run 4F/5P.
- **Vector Δ**: 0 modified + 9 added (`TestSensitiveData403BodyShapes`).
- **Twin retirement**: TS `2b72ce1` — internals.ts 403 branch → the Python
  post-fix expression twin; `pyTruthy` deleted.

### (d) OAuth error details carried the full 200 token payload

- **Root cause**: `_post_token_request` missing-required-fields branch put
  the FULL 200 token payload (live bearer material) into
  `OAuthError.details["response_data"]` — inbound-ledger row 1, shared by
  node refresh and browser exchange (TS core `oauth-http.ts:245`).
  Fix-of-record:
  `context/phase3/bug-reports/python-oauth-error-details-token-payload.md`.
- **Fix (three layers, all red-first)**: `57c5e16` (FIX-2) token-key
  redaction; ARB-A `20c7d6b` non-dict-200 record guard (crash regression the
  pair-A fidelity review caught); ARB-B `db8a33e` leak-class closure —
  ALLOWLIST redaction (only primitive `token_type`/`expires_in`/`scope`/
  `error`/`error_description` values render), non-JSON 200 never embedded
  (`content_type` + `body_length` only), non-object 200 → fixed placeholder.
  Non-200 branches (IdP error documents) intentionally untouched and
  vector-locked.
- **Vector Δ**: 0 modified + 1 added (refresh-path redaction vector); the 3
  exchange-path tests land in `raw_transport_no_entrypoint` (34→37).
- **Twin retirement**: TS `2b72ce1` + `b7152da` + `da68958`; browser README/
  JSDoc caveats rewritten (scrub-before-telemetry advice retained for
  non-200 bodies). R10.9 spot-harness through the shipped browser entry
  point: 17/0, `response_data` byte-equal node ≡ browser ≡ Python vector.

## 2. Re-pin (ONE event, commit `a1d43a5`)

Re-recorded via `just conformance-record` @ `700db996`; D8/D9 drift
accounting CLEAN — 150 bundles stamp-only, exactly the 20 disclosed vectors
modified, 11 added (each traced to a named new test), 0 removed,
0 unexplained. P3-0 re-measure: prefix deltas exactly `bookmark_builders`
134→135, `api_client` 810→819, `oauth_flow` 7→8. Both arbiter passes
produced ZERO vector flips (verified by full corpus replays) — the pin held
`700db996` through ARB-A and ARB-B; ONE re-pin event total, as ruled.

## 3. Differential regression (this gate's run, 2026-08-17)

Fresh seed **741097477** + replay of the ENTIRE B9 gate seed set
(1059451707, 3343231, 28631260, 52794688, 40075993, 53062695, 47824574,
628997442, 715310894, 419393897): **11/11 seeds `status: ok`, 28,091
examples / 0 skips / 0 divergences each**, exit 0, no repros written
(`repros/` still exactly the two RESOLVED P2-9 records). The four fixes
changed real behavior — expected divergences were exactly none because both
sides fixed identically, and that is what was observed. Raw JSONs committed
at `conformance/differential/oracle/2026-08-17-bugfix-gate-seed*.json`;
RUN.md section appended.

## 4. Referees (both FULLY CLEAN — first time in project history)

- **(a) ajv** (`npm run referee:bookmark`, TS repo): 9/9 green, 0 REJECT.
  Archived: `context/phase4/reports/2026-08-17-bugfix-gate-referee-a-ajv.txt`
  (`.txt` because the repo gitignores `*.log`; the TS conformance transcript
  is archived alongside as `…-ts-conformance.txt`).
- **(b) bookmark_parser** (analytics checkout READ-ONLY, pinned-wheel
  recipes): handoff regenerated BYTE-IDENTICAL; selftests `status: ok` both
  oracles FIRST; structural 314/314 ACCEPT exit 0; deep 125/0/189 exit 0.
  Fresh reports identical to committed `last-run-{structural,deep}.json`
  modulo `runtime_seconds` (working-tree copies restored; gate copies
  archived at
  `context/phase4/reports/2026-08-17-bugfix-gate-referee-b-{structural,deep}.json`).
- **No standing disclosed REJECT remains on either referee.** The disclosure
  pins were deleted, not allowlisted; any future REJECT is a NEW finding.

## 5. Docs updated by this gate

- `context/phase4/inbound-ledger.md`: rows 1 + 2 → **FIXED** with commit
  refs (additive; history kept) + ground-state addendum (3,262 @ 700db996).
- `context/phase3/design/phase3-playbook.md`: batch-CLOSED bullet appended —
  disclosure-registry state is now "no standing disclosed REJECT on either
  referee".
- Stale-count sweep re-run (on top of the repin-review's justified-survivor
  audit): remaining `3,251` / `70c904d` references in `context/` are all
  dated historical records (gate reports, notes, packet files, RUN.md
  checkpoints) — correct as history. TS repo docs: zero stale references
  (README already at 3,262; `batch-status.ts` carries the dated ADDENDUM
  from ARB-A R1). Memory doc note: the auto-memory
  `typescript-port-project.md` description line still says "corpus
  3,251/0/0" — outside the context/-only edit scope, flagged for the
  orchestrator.
- `conformance/differential/oracle/RUN.md`: gate section appended; 11 raw
  seed JSONs committed.
- PR body prepared: `context/phase4/notes/bugfix-pr-body.md` (stacked on
  `ts-port/phase2-contract-support` / PR #207 — merge after it).

## 6. Review convergence note (two blind pairs + arbiters)

Pair A (fidelity + repin) and pair B (security + e2e) reviewed the batch
BLIND and converged from opposite directions on the same ground truth:

- Pair A caught the CRASH on the bug-(d) redaction edge (non-dict 200 →
  uncoded AttributeError; ARB-A F1, guard applied red-first both repos).
- Pair B caught the LEAK CLASS in the surviving redaction (non-JSON-200
  verbatim embed, shallow top-level-exact-key redaction, verbatim non-dict
  rendering; ARB-B F-B1/F-B2/F-B3, allowlist fix applied red-first both
  repos). ARB-B's fix strictly contains ARB-A's — the record guard stayed,
  its locked rendering hardened verbatim → placeholder, flipped same-change
  on both sides (R10.2: strictly stronger assertions).
- Both pairs independently confirmed: corpus 3,262/3,262 @ 700db996 both
  languages, four twins retired, referees clean; and the
  `globalDataGroupId: string` spelling rests on static oracles only. Their
  two HUMAN-CALLs merged into ONE Phase-4 burn-in probe: save a bookmark
  with `data_group_id` per report type (insights + funnel + retention) and
  confirm the stored sections spelling round-trips (engine-read only by
  insights; funnel/retention emission is engine-inert per ARB-B E-3).
- Fixes (a)/(b)/(c): zero surviving findings in either pair (pair B
  re-derived the frequency server shape independently, byte-diffed 21
  builder payloads cross-language, fuzzed the 403 sniff 20-way — 0
  divergence).
- ONE re-pin event total; pin `700db996` held through both arbiter passes.

## 7. Branch discipline (confirmed at gate close)

- `git log c2a25c5..ts-port/python-bugfix-batch`: exactly the batch commits —
  `bddc576` (FIX-1), `95d16e2` (FIX-1 notes), `57c5e16` (FIX-2), `700db99`
  (FIX-2 notes = the pin commit), `a1d43a5` (RE-PIN), `2ea6442` (TS-FOLLOW
  notes), `20c7d6b` (ARB-A), `db8a33e` (ARB-B), plus this gate's docs
  commit(s). Nothing else; base `ts-port/phase2-contract-support` untouched;
  nothing on `main`.
- TS `main`: clean tree (no modified/staged files). One untracked,
  never-tracked `CLAUDE.md` (harness-generated repo guidance; prettier-
  formatted during TS-FOLLOW so `fmt:check` passes; deliberately left
  uncommitted — orchestrator may adopt or delete). TS batch commits over
  `origin/main`: `2b72ce1`, `b7152da`, `da68958` (plus the pre-batch docs
  rider `a382687` already on origin — ARB-A F3 ledger note).
- Push sequencing (ARB-A R2, for the orchestrator): publish the Python work
  branch BEFORE or together with TS main so corpus pin `700db996` never
  dangles remotely.

## 8. Environment / rules compliance

- All Python gates run as `env -u FORCE_COLOR -u COLORTERM just check`
  (FORCE_COLOR=3 harness gotcha).
- NO live API calls anywhere in the batch or this gate; NO mutation testing
  [SA1]; `/Users/jaredmcfarland/Developer/analytics` READ-ONLY throughout.
- uv-only Python invocation; local commits only.
