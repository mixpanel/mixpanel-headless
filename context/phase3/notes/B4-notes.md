# B4 batch notes — api_client + pagination (C1–C6, the wire client) · batch gate record

**Status**: GATE CLOSED (2026-08-16) — all P3-2e steps green, first attempt
**Model**: entire batch fable per the plan mandate (P3-3 B4 row; modules, bindings, harnesses, review pair, arbiter, gate)
**Date**: 2026-08-16
**Spec**: playbook v1.1 §P3-2e + §P3-5; packet `b4-packets.md` §Batch gate task / §Cautions / flip spec

## Module-task ledger (from shard notes; see per-shard files)

- C1 (`B4-C1-notes.md`): client core + wire-enablement seam (`createMixpanelClient`, `clientFromSession` + `context.state` memoization, me-selection, response-validation seam, fetch transport adapter) — TS `4f7bfa5`; 132 Layer-3 tests; 11 bindings; harness 39/39
- C2 (`B4-C2-notes.md`): query-host + streaming/export (24 names + the 3 B4 api-map members) — TS `99ba862`; 178 Layer-3 tests; harness 52/52
- C3 (`B4-C3-notes.md`): entity CRUD (dashboards + bookmarks-v2 + cohorts-app, 38 names) — TS `be74c38`; 105 Layer-3 tests; harness 65/65
- C4 (`B4-C4-notes.md`): flags + experiments + annotations + webhooks + alerts (46 names) — TS `e42c6c7`; 109 Layer-3 tests; harness 59/59
- C5 (`B4-C5-notes.md`): data governance + schemas + audit/anomalies/deletion + business context + replays signing (64 names) — TS `9305700`; 195 Layer-3 tests; harness 75/75
- C6 (`B4-C6-notes.md`): `pagination.py` whole file (`paginateAll` async generator) — TS `023cab5`; 44 Layer-3 tests; harness 56/56
- Review pair: `cb432d9` (wire-semantics lens, GO-WITH-FIXES: F1/F2 MAJOR + 3 minor + 1 nit) + `0e2b44d` (assertion-fidelity + deferral-closure lens, GO-conditional: 1 MAJOR + 2 minor); arbiter `3f0d3e2` / TS fixes `a24a58d` — **all 9 findings CONFIRMED, 6 code fixes red-first, deviation D-B4ARB-1 sanctioned, W-F6 injected-sleep pattern blessed — GO for gate** (`b4-review-resolution.md`)

## Gate checklist (P3-2e / packet §Batch gate task)

- [x] (1) **183-name assignment audit** (risk-register #1): union of the packet's six C-lists, sorted == `jq -r 'keys[]|select(startswith("api_client."))' corpus/api-index.json | sort` — **183/183, zero missing, zero duplicates, zero extras** (mechanical diff, clean). Every one of the 183 bound EXACTLY ONCE across `wire-{client,queries,entities,lifecycle,governance,pagination}.ts` (uniq -c over all literal registrations: max 1); the only additional flipped-prefix registrations are the B0-owned authored `api_client._iter_jsonl_lines` (bindings.ts, B0) and `pagination.paginate_all` (wire-pagination.ts). Re-implementation grep: zero local re-derivations of `_handle_response`/backoff/URL-building/header-merging in the shards — domain modules import the B0 exports by name (`handleResponse`/`executeWithRetry`/`appRequest`/`buildUrl`/`requestHeaders`); GATE-R5 grep re-run at the final tree: zero `response.json()`/bare `JSON.parse` on wire text in `packages/*/src` (comment/JSDoc mentions only).
- [x] (2) **batch-status flip**: `api_client.` + `pagination.` → done in the gate-checkpoint commit (single flip — the straggler ratchet; bound names already replayed while pending). The B0 exact-name entry `api_client._iter_jsonl_lines` kept as shadowed-but-consistent (packet flip spec). Header done/pending lists rewritten. **Standing collision assertion (P3-5 §4)** over all 424 distinct corpus api names (measured + setup): the two flipped entries capture exactly **185** names = the 183 api-index names + `api_client._iter_jsonl_lines` + `pagination.paginate_all` — ALL B4/B0-owned and ALL bound; **zero pending-batch names captured, zero longer `pending` overrides required**. Lock test: new "B4 gate flip" case in `batch-status.test.ts` (B2/B3 precedent); prefix-coverage cases green. One straggler test updated in `runner.test.ts` (setup-gating probe moved from the now-done `api_client.set_workspace_id` to pending `workspace.me` — the P3-1 † carried-vector shape, exactly as the C1 note's probe-migration comment predicted).
- [x] (3) **Conformance checkpoint — COUNTS MATCH EXACTLY**: `npm run conformance` with the flip: **3,251 vectors — 2,370 PASS / 0 FAIL / 881 UNPORTED** @ corpus `70c904dc598d` = 1,528 + **842** / 1,723 − 842. Gate delta 842 (not 843) attributed EXACTLY: 849 corpus vectors carry a flipped-prefix measured api (810 `api_client.*` + 39 `pagination.*`); 6 (`api_client._iter_jsonl_lines`) passed at B0; and mechanical scan finds exactly ONE vector with a pending-batch setup — `auth/api_client.resolve_workspace_id/test_workspace_resolution-testfacaderesolverwiring-test_resolves_from_me_cache_without_public_call` (measured `api_client.resolve_workspace_id`, setup `workspace.me`, a B6 member) — held UNPORTED per the P3-1 † footnote; it first passes at the B6 gate (B6 gate delta 354). 849 − 6 − 1 = 842. Report archived: `context/phase3/reports/2026-08-16-b4-gate.json`.
- [x] (4) **Oracle probes**: wire api names are EXEMPT (no oracle `call` surface — D14 / P3-2e item 3 / packet §Binding plan "nothing to register beyond the bindings"). B4 registered ZERO new oracle apis: `ALL_TARGETS` count unchanged at **45** families from B3 — the probe step is vacuously satisfied, stated for the record (B0-gate referee precedent).
- [x] (5) **Differential full-suite regression — ALL CLEAN, first attempt**: fresh seed **53062695** + replays of EVERY prior gate seed (**3343231** B2-fresh, **28631260** B0, **52794688** B0, **40075993** B3-fresh). All five: 45 families, **23,022 examples / 0 skips / 0 divergences** per seed, exit 0, no repros. Under-500 only the two documented finite-domain exhaustions (date_range 101, time_section 485). Raw JSONs `conformance/differential/oracle/2026-08-16-b4-gate-seed*.json`; RUN.md appended. `repros/` unchanged (the two RESOLVED P2-9 records).
- [x] (6) **B0-ARB carried items verified** (packet gate item 5): (a) R6.7 signal-aware closures landed WITHOUT B0 signature changes — diff of `client/{backoff,internals,app-request,headers,url,jsonl,scope}.ts` from the B0 tree (`613c8e6`) to HEAD shows only the sanctioned B0-ARB F1/F3 fixes (`pythonConstants` at wire-parse sites + `LosslessJsonError` guards) and one export-visibility widening (`jsonValuePythonStr`); no `signal` parameter on any B0 function — signal handling lives in the C1 assembly (`transport.ts` rawFetch, sleep-race wrapper) and C6's paginator; (b) every deviation-3 deferral landed — see §Deferral audit below.
- [x] (7) **Referees (a)/(b): NOT required at B4** (P3-7: bookmark-touching batches are B3/B6; C3's bookmark CRUD transports pre-built bodies and constructs none) — stated for the record per the B0-gate precedent.
- [x] (8) **Cleanup + checks**: `throwaway/b4-c1..c6` removed post-arbiter-sign-off (`3f0d3e2` GO pre-dates deletion; all six harnesses re-run at arbitration per `b4-review-resolution.md` §Ripples); eslint `throwaway/**` ignore glob + `.gitignore`/`.prettierignore` `throwaway/*/.build/` entries reverted (B0/B2/B3 precedent). `npm run check` green at the final TS tree (**130 files, 6,084 passed / 881 corpus-skips**, typecheck 5 packages, eslint, prettier, browser-bundle smoke OK); conformance re-verified 2,370/0/881 at the final tree; `just check` green (Python — notes/reports only).

## Deferral audit — B0 deviation-3 items (Caution #7 list), each verified landed

| Deferral (Python source) | TS lock (file:line, final tree) |
|---|---|
| `TestRetryStateResetRegression` ×4 (`test_api_client.py:1401-1572`) | `packages/core/test/client/client-export.test.ts:177` (`test_batch_count_resets_on_retry`), `:214` (`test_profile_page_count_resets_on_retry`), `:242` (`test_multiple_retries_dont_accumulate_state`), `:263` (`test_stream_rate_limit_error_carries_project_id`) |
| streaming `RateLimitError` `project_id` raise `api_client.py:1883-1891` (FF4 site 5; omits `response_body` only) | implementation `packages/core/src/services/queries/streaming.ts:387-394`; Layer-3 lock (the `test_api_client.py:1560-1575` / `:1567` assert) = `client-export.test.ts:263` |
| `test_export_events_negative_retry_after_uses_backoff` (`test_api_client.py:3810`) | `packages/core/test/client/client-export.test.ts:281` |
| `test_form_body_sent_as_form_encoded` content-type assertion (`test_app_api_client.py:316`) | `packages/core/test/client/client-request.test.ts:279` (adapter-owned, C1) + the B0-half formBody threading lock `app-request.test.ts:302`; C5's form endpoints are the production exercise |
| auth-header wire captures, Bearer/Basic end-to-end via `clientFromSession` | Basic: `client-authenticated-requests.test.ts:67` (+ the byte-exact recorded-header diffs on all 843 replayed vectors); Bearer per-request resolution: `client-core.test.ts:167-171`, `:457-458`, `:493` |

## Review-pair findings (summary — full detail in `b4-review-{wire,assertions}.md` + `b4-review-resolution.md`)

All nine CONFIRMED by the arbiter, none rejected: W-F1 exportEvents mid-stream retry gap (MAJOR, fixed — `guardedByteSource`); W-F2 dead `timeoutSeconds` (MAJOR, fixed — adapter timeout clock, **sanctioned deviation D-B4ARB-1**: a streaming body after headers is not clock-bounded, Python's per-read 600s budget has no fetch primitive); W-F3 abort-reason normalization (fixed); W-F4 pagination catch scope `except Exception` parity (fixed; comment mis-citation corrected); W-F5 `session_id` String-coercion (fixed, JsonValue threaded; disclosed >2^53 residual, owner B8-conditional); W-F6 real-timer nit (**ruled: injected-sleep-seam pattern BLESSED** — B5/B6 instruction recorded); A-F1 three untranslated `test_api_client.py` classes incl. the only `withProject` locks (MAJOR, fixed — 23/23 in `client-authenticated-requests.test.ts`); A-F2 packet class-owner under-enumeration (fixed — full 36-class owner table in `b4-packets.md` §Addendum, binding instruction to B5/B6 packet authors); A-F3 four `TODO(port)` markers now have recorded owners (`b4-review-resolution.md` §A-F3 table).

## Forward notes

- **B5 packet author (P3-5 §4, carried per packet instruction)**: the B5 gate's generated exact-name entry `workspace.list_bookmarks` (B5 member, ZERO corpus vectors) prefix-captures the B6 member `workspace.list_bookmarks_v2` (7 vectors) — B5 MUST add the longer override `workspace.list_bookmarks_v2` → `pending` or those 7 UNPORTED B6 vectors flip to FAIL_ERROR. Re-run the standing collision assertion after generating the 44-entry list.
- The P3-1 † carried vector (`api_client.resolve_workspace_id` × `workspace.me` setup) stays UNPORTED through the B5 gate too; it first passes at B6 (gate delta 354 there).
- B6-W3 review: re-examine Discrepancy #10 (`extra_forbidden` order) at the `_validate_bookmark_params_schema` consumer, per the B3 record.
- A-F2 binding instruction to B5/B6 packet authors: enumerate every Layer-3 row against the source file's COMPLETE `grep -n '^class '` list; every class gets an owner or a cited exclusion.
- W-F6 ruling for B5/B6: injected-sleep seam for ALL retry timing; real short delays only where wall-clock passage is itself the observable.
- Open R10.7 items carried: frequency-filter clause shape, dataGroupId int threading (B3), and the 403-TypeError branch (`python-handle-response-403-typeerror.md`, replicated NOT fixed at B0/B4 per R10.7 — Python-first fix + re-extraction remains pending, non-blocking).

## Tier attribution (gate observations)

B4 ran fable end-to-end (plan mandate) — no cross-tier attribution table applies (contrast B3). Observations for the record: the six shard tasks shipped 843/843 vector PASS with zero gate-time stragglers (the flip converted nothing); all nine review findings were review-lens discoveries in vector-unreachable territory (timeouts, mid-stream failure modes, untranslated lock coverage) — exactly the surface the P3-2d pair exists to cover, and an argument for keeping the pair mandatory even on all-fable batches. Both MAJORs were absence-of-behavior defects (no timeout clock, no mid-stream retry) that no corpus vector could reach: recorded-interaction replay cannot observe wall-clock or torn-body semantics (D1 limitation, known).

## Discovered facts / measurements

- Full-suite differential runtime holds at ~23s/seed (23,022 examples) — the 5-seed replay matrix costs ~2 minutes; replaying every prior gate seed stays standing posture through B6 (B3 recommendation re-confirmed).
- The flip's only test ripple outside `batch-status.test.ts` was the `runner.test.ts` setup-gating probe (predicted in the C1 notes): probe names for "mapped-but-unbound" semantics must migrate batch-by-batch as prefixes flip — B5/B6 gates should expect the same one-line migration (post-B6 the only pending probes left are `region_probe.`/`oauth_flow.`).
- 22 forward-setup vectors (workspace-measured with `api_client.*` setups) remain UNPORTED on their own measured-api pendency — will ride the B5/B6 flips (P3-5 §5, verified present in the 881).
